import { useEffect } from "react";

const DevOpsExcellence = () => {
  useEffect(() => {
    window.scrollTo(0, 0);
  }, []);

  return (
    <div className="min-h-screen bg-gray-50">
      // ... existing code ...
    </div>
  );
};

export default DevOpsExcellence; 